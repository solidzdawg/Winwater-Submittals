# Winwater Submittals — Python dependencies
#
# Install with:  pip install -r requirements.txt
#
# System requirement (not pip-installable):
#   libreoffice  — sudo apt-get install libreoffice

pypdf>=4.0
weasyprint>=61.0
openpyxl>=3.1
python-docx>=1.1
